# 安全策略

NodeControll 仍在开发阶段，当前没有受支持的生产版本，也不应暴露到不可信网络。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告。不要在公开 issue、discussion、日志或截图中披露凭据、真实主机信息、可复现利用链或尚未修复的漏洞。

报告尽量包含受影响 commit/组件、前置条件、最小复现、影响、建议缓解方式，以及是否已在真实系统使用。维护者会先确认收件，再根据影响和当前开发阶段协调修复与披露；在公开修复前请保密。

依赖漏洞、密钥泄露、认证绕过、Agent 任意命令执行、订阅越权、证书私钥暴露和 sing-box 配置注入均属于优先报告范围。普通缺陷与功能请求请使用公开 issue。
